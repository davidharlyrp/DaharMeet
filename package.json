{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "push": "git add . && git commit -m 'first' && git push -u origin main",
    "fe": "cd app && npm run dev",
    "be": "cd backend && npm start",
    "rebuild": "cd backend && git fetch origin main && git merge origin/main && docker compose down && docker compose up -d --build && cd .."
  },
  "dependencies": {},
  "devDependencies": {}
}